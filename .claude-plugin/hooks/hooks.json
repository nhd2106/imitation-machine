{
  "description": "Imitation Machine workflow bootstrap — injects orchestrator context in opted-in repos",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/.claude-plugin/hooks/bootstrap.sh\""
          }
        ]
      }
    ]
  }
}
