# Semver Guide

- `feat:` usually implies minor
- `fix:` usually implies patch
- `BREAKING CHANGE:` implies major
- `docs:` / `test:` / `chore:` alone usually imply patch or no release depending on policy

Check actual release intent before tagging.
